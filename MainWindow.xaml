<Window x:Class="BudgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetApp"
        xmlns:views="clr-namespace:BudgetApp.Views"
        xmlns:vms="clr-namespace:BudgetApp.ViewModels"
        Title="Budget Self" 
        Height="920"
        Width="1440"
        FontSize="14"
        mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:PeopleViewModel}">
                <views:PeopleView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:AccountsViewModel}">
                <views:AccountsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:IncomesViewModel}">
                <views:IncomesView/>
            </DataTemplate>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Close" Height="auto"/>
            <Button Grid.Column="2" Content="Settings" Height="auto"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="5 20 0 0">
                <Button
                    Command="{Binding NavigateToPeopleView}"
                    Content="People"/>
                <Button
                    Margin="0 20 0 0"
                    Command="{Binding NavigateToIncomesView}"
                    Content="Incomes"/>
                <Button
                    Margin="0 20 0 0"
                    Command="{Binding NavigateToAccountsView}"
                    Content="Accounts"/>
            </StackPanel>

            <ContentControl 
                Grid.Column="1"
                Margin="10 10 0 0"
                Content="{Binding CurrentViewModel}"/>
        </Grid>
    </Grid>
</Window>
